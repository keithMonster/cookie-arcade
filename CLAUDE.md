# cookie-arcade

Cookie 的家用游戏台：面向不识字幼儿的 mini game 合集，每款单文件 HTML、零网络、GitHub Pages 部署。本文件是**新增 / 改动游戏前必读的准入规则**。

## 玩家年龄（新游戏的第一变量）

- 锚点：Cookie 生日 **2024-07-05**。动手前先按今天日期算出当前月龄，再设计。
- 用当前月龄查这个阶段的发展窗口（语言 / 认知 / 精细动作 / 假装游戏 / 社交情感），新游戏落在**正在萌芽的能力**上——踮脚够得着，玩几次能看到进步。
- 两头都不做：**超龄**（认字、计分、对错惩罚、多步规则）和**已玩腻的形态**（纯乱按出反馈那类，是 1 岁半的杠杆，不再重复加）。

## 新增游戏准入四问（写不出答案不开工）

1. **盘点存量**：通读 README「Games」表全部条目，列一张覆盖矩阵——互动方式（拍键 / 点选 / 拖拽 / 擦除 / 掀开 / 涂画…）× 练什么（词汇 / 形状颜色 / 配对 / 物体恒存 / 生活习惯 / 音乐 / 假装游戏…）。新游戏必须**补一个空格**，或明确替换升级某一款；跟已有游戏只差皮肤的不做。
2. **定发展意义**：一句话说清「这款练什么」——学说话、认形状颜色、数量感、生活自理的假装演练、听指令找目标……这句话就是游戏的存在理由，也决定语音文案怎么写。**写不出、或写出来是「练手眼协调」这类放之四海皆准的话 → 去读 [`WHY.md`](WHY.md) 第四节**：那里有从全部存量倒推的能力线地图（哪几条已饱和、哪几条还空着）和三条判废标准。
3. **对齐年龄**：按上节推算的月龄核对——玩法在萌芽区吗？语音词汇量、节奏、等待时长配得上现在的理解力吗？
4. **算体验节奏**：单次停留 30s–3min 的注意力窗口内能完整玩一轮；**永远以成功收尾**——选错时摇头 / 晃动 / 语音引导，2 次后正确项发光，没有失败态。

## 设计底线（每款都守，历史承重事实）

> 这些底线的理由链、各自的**失效信号**，以及外部素材（别人家的游戏）怎么取舍 → [`WHY.md`](WHY.md)。清单只能被遵守，理由才能被推导；遇到清单没覆盖的边界情况先读它。

- 零文字 UI、零按钮菜单设置、零关卡 / 成就 / 进度。
- 每次输入都有反馈：形 + 色 + 音 + 动，四件套齐。
- 键盘、鼠标、触屏三输入都通；iPad safe-area 适配。
- 每款一个目录 `games/<name>/`，单文件 HTML；允许 CSS/JS 重复换独立 fork，重复未造成真实维护故障前不抽象。
- 零运行时依赖、断网可玩。
- 音量克制（效果音 ≤0.3 起步），保护幼儿耳朵。

## 语音契约（SSOT = tools/gen-audio.py + README「Audio」节）

- 所有中文语音构建期生成 mp3：文案写进 `tools/gen-audio.py` 的 manifest → `python3 tools/gen-audio.py --game <目录>` 生成 → 同命令 `--check` 通过再提交。
- 运行时不调 SpeechSynthesis（目标 iPad 上无声）；非语音效果音继续用 Web Audio。
- iOS 连续 / 自动接续语音复用单个 `Audio` 元素换 `src`，参考实现 `games/words/`。
- TTS 是付费接口：只为新增文案生成，不为「确认一下」重跑已有音频。**本仓语音生成已获 Keith 长期授权（2026-09-02：「不用问我，全部 ack」）——新增文案直接跑 `--game <目录>`，不再逐次请求确认**；授权范围止于本仓 manifest 里的新增条目，全量重生成仍先说一声。

## 交付清单（做完逐项勾）

1. `games/<name>/index.html` 落地，三输入实测。
2. 语音走上节契约，`--check` 通过。
3. 首页 `index.html` 登记 tile（动态 mini preview，不是静态图）。
4. README「Games」表（含标题款数）+ STORY.md 补条目 + **`WHY.md` 两张表给新款落位**（toy→game 轴分档、能力线；若它填上了某条空白线，把该行状态一并改掉）。
5. `python3 tools/check-docs.py` 通过 —— 机械核对款数三方一致、每款在地图上有位置、文档链接无死链。
6. 真机验证：目标 iPad / iPhone Safari 实测，**以扬声器出声为准**——桌面可解码、资源 200 都不算数。
7. 推 Pages 后线上入口点开复验（`.nojekyll` 已在根目录，勿删）。
